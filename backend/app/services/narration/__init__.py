"""Phase-2 domain card narration & rendering (DOMAIN_CARD_NARRATION_AND_RENDERING_SPEC).

Phase-2A structured core: the card-contract coverage matrix + eligibility gate (matrix.py), the narration
fact-source / adapter-capability registry (fact_source.py), the AZALEA_DOMAIN_NARRATION_V2 rollout-mode
resolver (rollout.py), and the per-domain within-card narration contracts (contracts.py). Pure + flag-gated;
the invasive blueprint/trace wiring + renderer spike are Phase-2B.
"""
